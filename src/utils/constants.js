import images from '../assets/images';

const { icons } = images;

const iconsArray = [
  icons.facebook,
  icons.twitter,
  icons.instagram,
]

const constants = {
  iconsArray,
}

export default constants;
