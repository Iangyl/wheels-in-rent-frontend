import { images } from '../assets';

const { icons } = images;

const iconsArray = [
  icons.facebook,
  icons.twitter,
  icons.instagram,
]

const constants = {
  iconsArray,
}

export default constants;
